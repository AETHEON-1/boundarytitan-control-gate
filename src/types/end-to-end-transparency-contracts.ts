export type TransparencyStage = 'INTAKE' | 'PRESERVATION' | 'CLASSIFICATION' | 'EVIDENCE' | 'ANALYSIS' | 'AUTHORITY' | 'CONSEQUENCE' | 'HUMAN_DISPOSITION' | 'CORRECTION' | 'CLOSURE' | 'REOPENING';
export interface TransparencyEvent { event_id: string; stage: TransparencyStage; occurred_at: string; actor_id: string; input_refs: string[]; output_refs: string[]; evidence_refs: string[]; authority_refs: string[]; owner_refs: string[]; unresolved_questions: string[]; content_hash?: string; }
export interface TransparencyChainInput { chain_id: string; events: TransparencyEvent[]; required_stages: TransparencyStage[]; consequence_owner_id?: string; external_stop_authority_id?: string; human_disposition_owner_id?: string; independent_evidence_custodian_id?: string; }
export interface TransparencyGap { gap_id: string; stage?: TransparencyStage; description: string; severity: 'HIGH' | 'MEDIUM' | 'LOW'; unsupported_arrow?: string; }
export interface TransparencyChainResult { chain_id: string; ordered_event_ids: string[]; gaps: TransparencyGap[]; first_unsupported_arrow?: string; continuity: 'CONTINUOUS_WITHIN_TESTED_BOUNDARY' | 'BROKEN' | 'INDETERMINATE'; authority_separated: boolean; human_disposition_required: true; machine_certification: false; machine_closure: false; }