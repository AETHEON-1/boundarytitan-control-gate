# Zombie Permission Hunter Prototype

Status: declaration-only, read-only prototype.

Repair tranche included:

- Type-safe owner field
- Multiple detected conditions preserved instead of overwritten
- NO_DECLARED_DEFECT_DETECTED separated from validity or cleanliness
- Invalid and inconsistent timestamps detected
- Uninspected surfaces preserved as NOT_TESTED
- Explicit translation-loss statement
- No inferred denial authority, revocation, approval, or closure

The prototype still does not inspect live systems, use credentials, revoke permissions, certify coverage, or declare the environment clean.

Open questions remain:

- Are dormant permissions intentional recovery or emergency access?
- Which permission classes may be auto-expired?
- Does revocation dominate cached, delegated, queued, and replicated access?
- Who actually holds denial and revocation authority?
- What evidence establishes registry coverage?
- What happens if fail-closed behavior harms recovery or safety?
- How are evidence custody and permission reality independently established?

Run npm run typecheck and npm test.

The prototype is preparation, not permission.

CAN ⊬ MAY.
