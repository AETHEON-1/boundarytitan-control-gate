{
  "name": "@boundarytitan/control-gate-phase1",
  "version": "0.1.0-phase1",
  "private": true,
  "description": "Phase 1 declaration-only TypeScript contracts and frozen-schema compatibility tests.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest --runInBand",
    "test:python": "python3 -m unittest -v test_agentic_handoff.py",
    "verify:state": "node scripts/verify-repository-state.js"
  },
  "devDependencies": { "@types/jest": "^30.0.0", "@types/node": "^20.17.30", "ajv": "^8.17.1", "ajv-formats": "^3.0.1", "jest": "^30.5.1", "ts-jest": "^29.2.6", "typescript": "^5.8.3" },
  "jest": { "preset": "ts-jest", "testEnvironment": "node", "testMatch": ["**/*.test.ts"] }
}
